# Copy to .env and fill in. .env is gitignored.

ANTHROPIC_API_KEY=
TAVILY_API_KEY=

# dworks ingest endpoint. From the local devcontainer this is the VPS over
# Tailscale; from the VPS cron it's the local dworks address.
# (Port TBD — confirm what dworks listens on. Use --dry-run until then.)
DWORKS_API_URL=http://100.93.140.39:3000
DWORKS_INGEST_TOKEN=
